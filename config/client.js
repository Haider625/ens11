// // eslint-disable-next-line no-undef
// const socket = io('http://localhost:6000'); // يجب تغيير عنوان الخادم بما يتناسب مع بيئتك

// // إرسال إشعار إلى الخادم
// socket.emit('sendNotification', { message: 'Hello from the client!' });

// // استقبال الإشعار من الخادم
// socket.on('receiveNotification', (data) => {
//     console.log('Received notification from server:', data.message);
// });
